# ============================================
# Portas do backup (evitar conflito com projeto original)
# ============================================
# Use com: docker compose -f docker-compose.backup.yml --env-file .env.backup up -d
# Ou copie para .env.backup e ajuste conforme necessário.

POSTGRES_HOST_PORT=5434
BACKEND_HOST_PORT=8001
FRONTEND_HOST_PORT=3001

# URL da API exposta ao browser (frontend backup chama backend na porta 8001)
NEXT_PUBLIC_API_BASE=http://localhost:8001
NEXT_PUBLIC_API_URL=http://localhost:8001
